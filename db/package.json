{
  "name": "db",
  "version": "1.0.0",
  "scripts": {
    "json-server": "json-server --watch cars.json -- port 3000",
    "dev": "concurrently \"npm run start\" \"npm run json-server\""

  },
  "devDependencies": {
    "concurrently": "^6.2.1",
    "json-server": "^0.16.3"
  },
  "dependencies": {
    "body-parser": "^1.19.0"
  }
}
